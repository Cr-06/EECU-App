# EECU-App
After this update, I still need to do the JS for all pages. Other than that I just have fixes like the cont. btn. on the second page and whatever happens with the 
third page. I also need to add the colors for the key for the pie chart, I want to put them underneath the expenses boxes in the third page, also want to make it where
you can only input numbers into inputs. A MAYBE feature is convert the jobs from the source code to a personal JSON file, only for fun if I want to add a bogey job to the list. 